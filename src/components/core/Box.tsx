import styled from "styled-components";
import {
  typography,
  layout,
  color,
  space,
  flexbox,
  border,
  backgroundColor,
} from "styled-system";

const Box = styled.div`
  ${typography}
  ${layout}
  ${color}
  ${space}
  ${flexbox}
  ${border}
`;

export default Box;
